# Add these in Vercel Project Settings → Environment Variables. Never commit values.
GEMINI_API_KEY=
GEMINI_COACH_MODEL=gemini-3.1-flash-lite
DATABASE_URL=
# Generate a random value with at least 32 characters; use the same value in Preview and Production.
COACH_SESSION_SECRET=
